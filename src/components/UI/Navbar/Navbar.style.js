const badgeProp = {
  m: 0.5,
  display: "none"
};

const mLR = {
  ml: 0.5,
  mr: 0.5
};

const onHoverVal = {
  "&:hover": {
    cursor: "pointer",
    color: "#E2FF6F"
  }
};

const menuIcon = { fontSize: "9px", height: "20px", margin: 0.3 };
const kycTag = { fontSize: "0.9rem" };
export { badgeProp, mLR, menuIcon, kycTag, onHoverVal };
